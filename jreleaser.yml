project:
  languages:
    java:
      groupId: net.aluminis

signing:
  active: 'ALWAYS'
  armored: true

deploy:
  maven:
    mavenCentral:
      central:
        active: ALWAYS
        url: https://central.sonatype.com/api/v1/publisher
        stagingRepositories:
          - target/staging-deploy